import { Entity, Column, PrimaryColumn } from 'typeorm';

@Entity()
export class TUTOR {
  @PrimaryColumn({ length: 10 })
  Tutor_id: string; // Khóa chính khớp với Tutor_id trong STUDENT

  @Column({ length: 30 })
  TUTOR_NAME: string;

  @Column({ length: 30, nullable: true })
  EMAIL: string;
}
